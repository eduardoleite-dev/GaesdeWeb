@page "/login"
@layout EmptyLayout
@inherits LoginPage

<PageTitle>Login | GAESDE</PageTitle>

<div class="login-page">
    <section class="login-banner">
        <div class="banner-brand"><span class="brand-mark">G</span> GAESDE</div>
        <div class="banner-copy">
            <p class="eyebrow">@BannerEyebrow</p>
            <h1>@BannerTitle<br /><em>@BannerHighlight</em></h1>
            <p>@BannerDescription</p>
        </div>
        <div class="banner-quote">“Toda pergunta pode abrir um novo caminho para aprender.”</div>
    </section>

    <section class="login-panel">
        <div class="login-panel-inner">
            <p class="eyebrow">ENTRAR NO GAESDE</p>
            <h2>Vamos criar juntos</h2>
            <p class="login-subtitle">Games, estudo e desenho para explorar novas ideias.</p>

            <form @onsubmit="HandleLoginAsync" @onsubmit:preventDefault>
                <label for="username">Usuário</label>
                <input id="username" @bind="username" autocomplete="username" placeholder="usuarioMaster" disabled="@loading" />

                <label for="password">Senha</label>
                <input id="password" type="password" @bind="password" autocomplete="current-password" placeholder="••••••••" disabled="@loading" />

                @if (!string.IsNullOrWhiteSpace(errorMessage))
                {
                    <p class="error" role="alert">@errorMessage</p>
                }

                <button class="button primary full-width" type="submit" disabled="@loading">
                    @(loading ? "Entrando..." : "Começar conversa")
                </button>
            </form>
        </div>
    </section>
</div>